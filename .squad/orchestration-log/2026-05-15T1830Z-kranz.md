# Orchestration entry — Kranz

**Timestamp:** 2026-05-15T18:30:00Z
**Agent:** Kranz (Build & Release)
**Requested by:** Kyle Poineal
**Mode:** background (multi-agent fan-out)
**Why routed:** Packaging feasibility was the decisive constraint on framework selection. Invariant #12 (Debian native packaging via `/usr/share/nodejs`) means any candidate framework must be evaluated against the .deb build path before adoption.

**Input artifacts authorized to read:**
- `AGENTS.md` (invariant #12)
- `contrib/debian/control`, `contrib/debian/rules`, `contrib/debian/changelog.in`
- `src/web-app/webpack.config.js` (look for `USE_SYSTEM_NODEJS_MODULES`)
- `src/web-app/BuildWebApp.cmake`
- `CMakeLists.txt`
- `.squad/decisions.md`
- `.squad/decisions/inbox/copilot-directive-20260515-rules.md`

**Files produced:**
- `.squad/decisions/inbox/kranz-ui-poc-packaging.md` (verdict table)
- `.squad/agents/kranz/history.md` (appended)

**Outcome:** Per-framework packaging verdicts published — React ✅ (status quo); Svelte vanilla ⚠️ (`node-svelte` / `node-svelte-loader` need verification in Debian trixie `/usr/share/nodejs`, manageable but may require pre-build pattern); SvelteKit ❌ (vite/rollup absent, requires committed `dist/` — significant `debian/rules` rewrite); Flutter Web ⚠️ (Dart SDK not in Debian build path, pre-build-and-ship only); Flutter Desktop ❌ (own renderer breaks Tauri embedding contract — replaces the desktop binary entirely). POC scope narrowed to Svelte vanilla and/or Flutter Web with Flutter Desktop hard-blocked.
