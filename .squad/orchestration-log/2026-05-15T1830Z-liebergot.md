# Orchestration entry — Liebergot

**Timestamp:** 2026-05-15T18:30:00Z
**Agent:** Liebergot (C++ Server Core)
**Requested by:** Kyle Poineal
**Mode:** background (multi-agent fan-out)
**Why routed:** With `lemond` frozen for the POC, the team needed a canonical, immutable contract describing exactly what the new UI must talk to. Liebergot owns the server side and is the only agent authorized to author that contract.

**Input artifacts authorized to read:**
- `AGENTS.md`, `CLAUDE.md`, `DESIGN.md`
- `src/cpp/server/server.cpp` (route registration table, web-mock injection)
- `src/cpp/server/router.cpp`
- `src/cpp/include/lemon/websocket_server.h`
- `src/cpp/include/lemon/recipe_options.h`, `model_types.h`, `config_file.h`
- `src/cpp/server/anthropic_api.cpp`, `ollama_api.cpp`
- `src/app/src/renderer/tauriShim.ts` (window.api shape)
- `.squad/decisions.md`
- `.squad/decisions/inbox/copilot-directive-20260515-rules.md`

**Files produced:**
- `.squad/decisions/inbox/liebergot-ui-server-contract.md` (REST + WebSocket + window.api + auth + CORS + invariant fences)
- `.squad/agents/liebergot/history.md` (appended)

**Outcome:** Single source-of-truth contract document delivered with seven sections: topology, REST inventory (with quad-prefix invariant and full endpoint table by domain), WebSocket Realtime API (incl. `websocket_port` discovery), the `window.api` shim contract (identity, window controls, native bridges, per-client settings, server discovery, misc, web-mock injection point), auth/CORS, MUST-NOT list (six fence-posts), free-for-the-UI list, and a Mattingly-facing summary. No `lemond` code changed. The document is the canonical spec the new UI MUST honor; deviation requires a new decision.
