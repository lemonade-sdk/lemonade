# Orchestration Log — 2026-05-15T21:15:00Z — Recipes v1.2 (Research → Design → Build)

**Requested by:** Kyle Poineal
**Coordinator:** Squad

## Agents spawned

### 1. Explore agent — recipe system research (read-only)

- **Mode:** sync (read-only inputs feeding Kyle + coordinator's design synthesis)
- **Why chosen:** Kyle proposed a "personas" concept for backend tuning. Before
  proposing UI, needed to ground the design in lemond's actual contract — what
  the C++ codebase already calls "recipe," what's load-time vs request-time,
  and where precedence is resolved.
- **Files authorized to read:**
  - `src/cpp/include/lemon/recipe_options.h`
  - `src/cpp/server/model_manager.cpp`
  - `src/cpp/resources/server_models.json`
  - `src/cpp/resources/backend_versions.json`
  - `src/cpp/include/lemon/wrapped_server.h`
  - `src/cpp/include/lemon/server_capabilities.h`
  - `AGENTS.md`
- **Files produced:** none (research only — output returned inline to Kyle and
  coordinator)
- **Outcome:** Mapped the `recipe` concept: in code, a recipe = the backend
  engine id (`llamacpp`, `flm`, `ryzenai-llm`, `whispercpp`, `sd-cpp`, `vllm`)
  with a per-engine JSON option schema. Sampling params are a separate,
  request-level concern. Identified the 4-layer precedence chain (global →
  model-json → user-saved → per-request). This research directly informed the
  two-tier UI data model (portable Recipe vs AppliedRecipe) and the
  engine-config-vs-sampling section split in the slide-over.

### 2. Mattingly — Recipes UI build (v1.2)

- **Role:** UI / Frontend
- **Mode:** sync (single-author prototype build, dependent on Kyle's terminology
  call before naming the surface)
- **Why chosen:** Owns `prototype/ui-redesign/`; just delivered v1.1 audit pass;
  has full context on the design system (tokens, components, IA decisions).
- **Files authorized to read/modify:**
  - `prototype/ui-redesign/index.html`
  - `prototype/ui-redesign/styles.css`
  - `prototype/ui-redesign/app.js`
  - `prototype/ui-redesign/tokens.css` (read only — verified unchanged)
- **Files produced/changed:**
  - `prototype/ui-redesign/index.html` — top nav `+Recipes`, recipes view
    section, chat composer pill + switch-recipe popover, model slide-over
    recipe section, recipe slide-over aside.
  - `prototype/ui-redesign/styles.css` — v1.2 block (clearly delimited):
    recipes layout, recipe-card, phase-glyph (◐ inline SVG), applied-list,
    dropdown, drop overlay, recipe-slideover form components (field / slider /
    select / input / textarea / disclosure), chat-pill, recipe-popover,
    row__recipe-chip, model-recipe, toast.
  - `prototype/ui-redesign/app.js` — `STARTERS` / `YOURS` / `MODEL_ENGINES` /
    `appliedRecipes` data, render functions, slide-over open/close, slider
    mirroring, drag-drop visual, popover, model slide-over recipe wiring,
    toast.
  - `.squad/decisions/inbox/mattingly-recipes-v1-2.md` — decision drop (since
    merged into `decisions.md` by Scribe).
  - `.squad/agents/mattingly/history.md` — appended v1.2 learnings.
- **Outcome:** Recipes surface delivered. Six bundled starters (Balanced,
  Quality, Fast, Creative, Long Context, Code) + two mock user recipes +
  applied-to-models zone. Slide-over splits engine config (applied at `/load`)
  from sampling (applied per `/chat/completions`). Compatibility tooltips on
  apply dropdowns. Drag-drop import overlay (visual only). No new design
  tokens. tokens.css unchanged. Three open judgment calls flagged for Kyle:
  starter mutability, Save-as-Recipe engine seeding, schema docs timing.
- **Intentional deviation noted:** Used JS lookup `MODEL_ENGINES` keyed by
  `data-name` instead of adding `data-engine` attrs to every model row.
  Acceptable for prototype; production port should source engine from the
  model registry response.

## Coordinator notes

- **Terminology call (Kyle):** UI keeps "Recipes" name despite collision with
  code-level engine-id meaning. Code/contributor docs will re-label the
  engine-level concept as "engine type" or "backend" going forward.
- **Invariant #11 honored:** Recipes are a client-side concept. No `lemond`
  endpoint changes. No server-side storage.
- **No `main` merge.** Branch stays `feat/ui-testing`.
- **No backend touched.** No changes to `src/cpp/`, `src/app/`, `src/web-app/`.
