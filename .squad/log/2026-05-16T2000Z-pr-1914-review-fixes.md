# 2026-05-16T20:00:00Z — PR #1914 review-feedback fixes

**Requested by:** Kyle Poineal
**Agents:** Kranz (sync), Scribe (background)
**Branch:** `fix/1885-bug-report-template-commands` (PR #1914, fixes #1885)

## What happened

@jeremyfowers requested changes on PR #1914 (bug report template) with three inline comments. Kranz applied all three fixes in commit `976a8260` and pushed:

1. Removed redundant "Start the server with `lemond`" step (server auto-starts).
2. `lemond --log-level debug` → `lemonade config set log_level=debug` (no restart needed).
3. `lemonade recipes` → `lemonade backends`.

CLI syntax verified against C++ source — citations recorded in the decision entry.

## Source citations (for future CLI/docs work)

- `lemonade config set KEY=VALUE` — `src/cpp/cli/main.cpp:1041` (uses `allow_extras(true)`)
- `log_level` config key — `src/cpp/server/runtime_config.cpp:351`, `src/cpp/server/config_file.cpp:187`
- Hot-reload path — `src/cpp/server/logging_config.cpp:137` (`reconfigure_application_logging()`)
- `lemonade backends` — `src/cpp/cli/main.cpp:1031`

## State

- PR branch pushed; awaiting maintainer re-review.
- Working tree returned to `feat/ui-testing` clean (prototype/ untracked as expected).
- No `.squad/` files staged or committed on PR branch.
- Liebergot's history updated with CLI-verification references (cross-agent share).

## Open

- Kyle deciding: confirm-comment-on-PR vs leave-for-GitHub vs file-follow-up-for-restart-docs.
- Potential follow-up: `docs/server/` page on restarting the server (not required for #1914).
